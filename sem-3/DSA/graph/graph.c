#include <stdio.h>
#include<stdlib.h>

typedef struct graph Graph;

Graph *graphCreate(int n){

}

void graphADDEdge(Graph *g , int u, int v){
    // malloc here for the size of graph
    // link the var such that the link points to a linked list from the array
    // and then append the array by incrementing the pointer
}


